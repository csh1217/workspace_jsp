<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.joonzis.mybatis.mapper.employees">
	
	<select id="select_all" resultType="org.joonzis.vo.EmployeeVO">
		select * from employees order by employee_id
	</select>
	
	<select id="select_deptId" parameterType="String" resultType="org.joonzis.vo.EmployeeVO">
		select * from employees where department_id=#{department_id} order by employee_id
	</select>
	
	<select id = "select_dynamic" parameterType="map" resultType="org.joonzis.vo.EmployeeVO">
		select * from employees where
		<choose>
			<when test="key == 1">employee_id=#{value}</when>
			<when test="key == 2">first_name=#{value} order by employee_id</when>
			<when test="key == 3">department_id=#{value} order by employee_id</when>
		</choose>
	</select>
</mapper>






